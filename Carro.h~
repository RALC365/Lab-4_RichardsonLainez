#include <string>

using namespace std;

#ifndef CARRO_H
#define CARRO_H

class Carro{
	//atributos o metodos privados
	private:
		
	//metodos publicos
	public:
		Carro();
		Carro(string, string, int);
		string color;		
		string marca;
		int altura;
		//administracion
		
	//destructor
	~Carro();
};

#endif
